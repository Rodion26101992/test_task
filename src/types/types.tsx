export default interface IUser {
    id: string
    first_name: string
    last_name: string
    gender: string,
    birth_date: string,
    job: string,
    biography: string,
    is_active: boolean,
}